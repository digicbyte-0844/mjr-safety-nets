{
  "scripts": {
    "start": "firebase serve",
    "dev": "firebase serve",
    "deploy": "firebase deploy",
    "deploy:cf": "npx wrangler pages deploy src --project-name=mjr-safety-nets",
    "dev:cf": "npx wrangler pages dev src"
  },
  "dependencies": {
    "firebase-tools": "^15.5.1",
    "wrangler": "^3.114.0"
  }
}
